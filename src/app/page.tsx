// Root page uses the same component as catch-all for consistent behavior
import DynamicPathPage from "@/components/DynamicPathPage";

export default function Home() {
  return <DynamicPathPage />;
}
